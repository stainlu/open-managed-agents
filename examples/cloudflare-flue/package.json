{
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "deploy": "wrangler deploy",
    "dry-run": "wrangler deploy --dry-run",
    "dev": "wrangler dev",
    "preflight": "node scripts/preflight.mjs",
    "smoke": "node scripts/smoke.mjs",
    "smoke:state": "node scripts/smoke.mjs --check-state-readback",
    "smoke:sandbox": "node scripts/smoke.mjs --check-sandbox-exec",
    "smoke:task": "node scripts/smoke.mjs --check-flue-task",
    "smoke:promotion": "node scripts/smoke.mjs --promotion",
    "smoke:verify-report": "node scripts/verify-promotion-report.mjs",
    "types": "wrangler types"
  },
  "dependencies": {
    "@cloudflare/sandbox": "^0.10.1",
    "@flue/sdk": "^0.5.1",
    "open-managed-agents": "file:../.."
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250410.0",
    "typescript": "^5.9.2",
    "wrangler": "^4.83.0"
  }
}
